# bettor-desktop — Tauri port plan

Port `~/Code/bettor-calculator-main` (Next.js 16 / React 19, 21 calculators, ~2,800 lines of TS math)
to a Tauri v2 desktop app with **all math in Rust**. Add three new capabilities on top.

Source of truth for behavior: the existing TS. Source of truth for *math*: the new Rust crate.
No math ships in TypeScript. No Python anywhere.

---

## Architecture

```
bettor-desktop/
├─ Cargo.toml                  # workspace root
├─ crates/
│  └─ bettor-core/             # pure math lib — NO tauri, NO io
│     ├─ src/
│     └─ tests/
│        ├─ parity/            # golden vectors extracted from the TS implementation
│        └─ props/             # property-based tests (proptest)
├─ src-tauri/                  # thin shell: commands, sqlite, window mgmt
│  └─ src/
├─ src/                        # SvelteKit (adapter-static, SSR off, Svelte 5 runes)
│  ├─ lib/
│  │  ├─ bindings.ts           # AUTO-GENERATED by tauri-specta — never hand-edited
│  │  ├─ charts/               # D3 render fns
│  │  └─ ui/                   # design-system components ported from Next app
│  └─ routes/
└─ tasks/
```

**Key rule:** `bettor-core` has no Tauri dependency. It compiles and tests standalone with
`cargo test -p bettor-core`, which keeps the math testable without spinning up a webview.

**Type safety across the IPC boundary:** `tauri-specta` generates `src/lib/bindings.ts` from the
Rust command signatures. TypeScript never re-declares a shape the Rust already knows. Drift becomes
a compile error rather than a runtime `undefined`.

**Determinism:** the TS uses `Math.random()`, so its simulations are unreproducible and untestable.
Rust uses a seeded `ChaCha8Rng`. Every Monte Carlo command takes an optional `seed`. Same seed →
identical output, which is what makes the parity tests below possible at all.

---

## Module map: TS → Rust

| TS source | Rust module | Notes |
|---|---|---|
| `odds.ts`, `formatting.ts` | `odds.rs` | American/decimal/fractional, implied prob. Parse returns `Result`, not `null`. |
| `probability.ts` | `probability.rs` | `normal_cdf`, `inverse_normal_cdf`, `prob_to_spread`, `prob_to_beta` |
| `hold.ts`, `vigComparison.ts` | `hold.rs`, `vig_comparison.rs` | |
| `devig.ts` | `devig.rs` | 5 methods: EM, MPTO, Shin, OR, LOG. Shin/OR/LOG need root-finding — port the solver, don't hand-wave it. |
| `distributions.ts` | `distributions.rs` | Poisson / nbinom / gamma / lognormal sampling, histogram, ECDF, stats |
| `poisson.ts`, `nbinom.ts` | `poisson.rs`, `nbinom.rs` | PMF/CDF, score matrices, market derivation |
| `bayesian.ts` | `bayesian.rs` | prob/spread update, Dirichlet, 2-way + 3-way devig, edge calcs |
| `regression.ts` | `regression.rs` | shrinkage, confidence intervals, convergence series |
| `altline.ts`, `bestline.ts` | `altline.rs`, `bestline.rs` | line ladders, CDF line comparison |
| `middle.ts`, `teaserEv.ts` | `middle.rs`, `teaser_ev.rs` | teaser keeps the football key-number table `[3,7,10,14]` |
| `parlayCorrelation.ts` | `parlay_correlation.rs` | |
| `riskOfRuin.ts` | `risk_of_ruin.rs` | Monte Carlo + survival curve |
| `*Config.ts`, `sportDefaults.ts` | `config/*.rs` | sport constants as `&'static` tables |
| **inline in TSX** | `ev.rs`, `kelly.rs`, `arbitrage.rs`, `hedge.rs`, `parlay.rs`, `clv.rs` | must be **extracted** from the components first — see Phase 1.5 |
| *(new)* | `variance.rs` | odds-range / breakeven / drawdown module |
| *(new)* | `betlog.rs` (in `src-tauri`) | rusqlite, WAL |

---

## Verification strategy

This is the part that makes the port trustworthy, so it comes first, not last.

1. Write a Node harness that imports the **existing TS modules** and dumps golden vectors to JSON —
   a wide sweep of inputs per function, including edge cases (odds at ±100, prob 0/1, empty arrays,
   negative lines, degenerate distributions).
2. `bettor-core/tests/parity/` loads those JSON fixtures and asserts the Rust matches within a
   documented epsilon (`1e-9` for closed-form, looser for iterative solvers — stated per test).
3. Any place the Rust *intentionally* differs from the TS (a TS bug, or `null` → `Result`) gets an
   explicit documented exception in the fixture, not a silently loosened epsilon.
4. Property tests for invariants the fixtures can't cover: devig output always sums to 1.0;
   odds round-trip American→decimal→American; every PMF sums to ~1.0 over its support; CDF is
   monotonic; arbitrage stakes always sum to the total.

Definition of done for a module = fixtures pass + properties pass + `clippy -D warnings` clean.

---

## Phases

### Phase 0 — Scaffold  ← COMPLETE (pending visual confirmation of the window)
- [x] Tauri v2 + SvelteKit scaffold (`create-tauri-app`, svelte-ts template)
      → adapter-static, Svelte 5, TS, `@tauri-apps/cli` as devDependency
- [x] Cargo workspace at root; `crates/bettor-core` created and wired into `src-tauri`
- [x] `MathError` defined — replaces the TS `null`-on-any-failure pattern
- [x] SSR off + prerender in `+layout.ts`
- [x] D3 + KaTeX installed (+ `@types/*`); esbuild build script allowed via
      `pnpm.onlyBuiltDependencies` (pnpm 10 blocks postinstall by default)
- [x] `pnpm verify` = clippy `-D warnings` → `cargo test` → `svelte-check`
- [x] Smoke path: `engine_info` command proves SvelteKit → IPC → bettor-core
- [x] **Gate passed:** clippy clean, tests pass, svelte-check 0 errors,
      `vite build` succeeds, `pnpm tauri dev` launches the binary

Decisions made during Phase 0:
- **Edition 2021, not 2024** — matches the Tauri template; no upside to diverging.
- **No `panic = "abort"`** in the release profile — it breaks `cargo test --release`.
- **Strict clippy on `bettor-core` only.** `unwrap_used`, `expect_used`,
  `indexing_slicing`, `panic`, `float_cmp` are all `deny`: bad user input must
  return `MathError`, never unwind a desktop app. The shell crate can't take the
  same set because the Tauri macros generate code that trips the restriction lints.
- **Cast lints are `warn`, not `deny`** — `usize as f64` has no safe `From`, so
  denying would mean `#[allow]` noise everywhere rather than real rigor.
- **`opt-level = 3` for deps and `bettor-core` even in dev** — otherwise Monte
  Carlo in `tauri dev` is unusably slow.
- **Golden-vector fixtures are committed, not gitignored** — regenerating them on
  demand would let a regression rewrite its own expected output.

### Phase 1 — Rust core, tier 1 (closed-form math)  ← COMPLETE
- [x] `tools/gen-fixtures.mjs` — runs the original TS under Node 25 (native type
      stripping) and dumps 567 golden vectors to `tests/fixtures/`
- [x] `odds.rs`, `probability.rs`, `hold.rs` (+ vig comparison), `devig.rs`
- [x] 38 unit tests + 6 parity suites, all green; clippy `-D warnings` clean

**Parity results — 564 cases replayed against the TypeScript:**

| Fixture | Identical | Declared divergences |
|---|---|---|
| `probability.ts` | 132 / 132 | 0 |
| `odds.ts` | 284 | 24 |
| `devig.ts` | 83 | 21 |
| `hold.ts` | 14 | 1 |
| `vigComparison.ts` | 5 / 5 | 0 |
| `shin` | *rewritten* | differs on 17/18 valid markets |

An undeclared mismatch fails the build. A *stale* declaration — one where the
values now match — also fails, so the divergence list cannot rot.

**Bugs found in the TypeScript** (all fixed here, all with regression tests):

1. **`devigShin` was not Shin.** `q/S` inside the radical where Shin (1993)
   has `q²/S`. The bisection had no interior root, so `z` pinned to the search
   ceiling on every input and the method silently returned the same numbers as
   `devigMPTO` — the app offered five devig methods and two were duplicates.
   On a -1000/+500 market the longshot's fair probability moves 0.1550 → 0.1288.
2. **American odds were never range-checked.** `toDecimal("-1.5","american")`
   returned 67.67. Typing a spread into an odds field produced a plausible price.
3. **`devigEM([0, 0.5])` → `[0.25, 0.75]`** — invented 25% for an outcome the
   book priced at zero.
4. **Every method "devigged" a book summing below 1.0.** `[0.30, 0.30]` became a
   confident `[0.50, 0.50]`. That book is an arb, not a vigged market.
5. **`devigOR` gave up at `c > 50`** with a bracket capped at `hi < 100`, so it
   returned null for markets whose true exponent was larger. The answer for
   `[0.9999, 0.9999]` is an ordinary `[0.5, 0.5]`.
6. **`fromDecimal` returned probability on a 0–100 scale** from a module where
   every sibling returned 0–1, and emitted the fraction `"0/1"` for short prices.
7. **Solvers never reported non-convergence** — they returned the last bisection
   iterate either way. Now `MathError::NoConvergence` carries the residual.

**Bug found in my own port**, caught by the parity harness: the bisection loops
tested for convergence *before* narrowing the bracket, where the TS narrows
first. Same algorithm, final midpoint off in the last few ulps. Would never have
surfaced without golden vectors.

Kept deliberately identical: the Abramowitz & Stegun and Acklam approximations,
including the TS's truncated constants (1.5e-7 error at z=0 rather than the
published 7.5e-8). These price lines — changing them, even toward more accuracy,
would silently move every spread the app has quoted. That is its own change.

### Phase 1.5 — Extract inline math  ← COMPLETE
- [x] Lifted the math out of the `useMemo` blocks in EV / Kelly / Arbitrage /
      Hedge / Parlay / CLV
- [x] **Four** modules, not six — Kelly belongs with EV (`wager.rs`), hedging
      with arbitrage (`arbitrage.rs`); plus `parlay.rs` and `clv.rs`
- [x] 34 new unit tests (72 total in the crate), clippy clean

No golden vectors here: the source math was entangled with React state and
`.toFixed()` formatting, so there was no callable function to capture. These
modules are covered by behavioral tests against hand-computed values instead,
and every divergence below is asserted by a named test.

**Bugs found:**

1. **`CLVCalculator` displayed one number under three labels.** "Closing Line
   Value" `b/c−1`, "Edge (cents per dollar)" `(b−c)/c`, and "Expected Value"
   `(1/c)·b−1` are the same expression. Three rows, one value.
2. **That value is a ratio of decimal odds, which flatters longshots.** It
   ranks +400→+350 (11.1%) above -110→-130 (7.9%); in probability points it is
   2.22 vs 4.14 — the opposite order. `clv.rs` now reports `prob_points`,
   `cents`, and `ratio` side by side, plus `ratio_to_points_distortion()`.
   **This is the core demonstration the whole project is for**, and the
   existing calculator actively teaches the misconception.
3. **CLV's EV never removed the vig** — `trueProbability = 1/closeDecimal` takes
   the raw closing price as truth. Betting the exact closing price reports 0%
   EV when the true figure is −4.5%. `ev_vs_fair` needs the opposing close and
   returns `None` without it rather than guessing.
4. **`ParlayCalculator` silently dropped unparseable legs** and priced the
   parlay from the survivors — enter five legs, typo one, get a four-leg price
   presented as yours. Now an error.
5. **`ArbitrageCalculator` reported "guaranteed profit" on markets that do not
   arb** — a confidently-labelled negative number. Now `Option`, gated on
   `is_arb()`.
6. **`if (!stake)` passed negative stakes** in EV, Kelly, Arbitrage, and Hedge,
   since `!(-500)` is `false`.

Also added, not in the original: `Hedge::worst_case` (what you actually lock in,
rather than making the reader compare two numbers) and a documented caveat that
parlay multiplication assumes leg independence.

### Phase 2 — Rust core, tier 2 (distributions, models, simulation)  ← IN PROGRESS
- [x] `distributions.rs` — Poisson / negative binomial / gamma / lognormal
      samplers, histogram, over-probability, Welford stats
- [x] `risk_of_ruin.rs` — seeded Monte Carlo, rayon across paths
- [ ] `poisson.rs`, `nbinom.rs` — score matrices, market derivation
- [ ] `bayesian.rs`, `regression.rs`, `altline.rs`, `bestline.rs`, `middle.rs`, `teaser_ev.rs`,
      `parlay_correlation.rs`, `risk_of_ruin.rs`
- [ ] Sport config tables
- [x] `rayon` for the Monte Carlo paths
- [ ] Benchmark vs the JS to quantify the win

**Simulation code cannot be parity-tested.** The TS calls `Math.random()`, so
its output is not reproducible even against itself. These modules get
distributional tests instead (mean, variance, skew against closed forms) plus
determinism tests, which the seeded RNG makes possible for the first time.

**`bettor-core` never reads entropy.** Every simulation takes an explicit seed
and reports it back in the result, so any figure a user quotes can be
reproduced exactly. Generating a seed is the shell's job.

**Bugs found:**

1. **`samplePoisson` used a rounded normal approximation above λ = 30** while
   its doc comment claimed Ahrens-Dieter rejection. A normal is symmetric, a
   Poisson is right-skewed — the approximation flattened exactly the tail a
   prop line sits in. A regression test now asserts skew ≈ 1/√λ at λ = 50.
2. **`simulateRuin` mixed populations in adjacent statistics.** Median over
   *survivors*, mean over *all paths* with ruined scored as zero, displayed
   side by side. At a 30% ruin rate the median looks healthy while the mean is
   dragged down and nothing explains the gap. Now `median_ending_all`,
   `mean_ending_all`, and `median_ending_survivors`, each named for its base.
3. **Invalid input returned a zeroed result**, so a typo rendered as
   `0% risk of ruin` — the safest-looking answer possible.
4. **`generateSamples` returned an array of zeros** for a lognormal with
   `mu <= 0`, which charts as certainty at zero.

Added, not in the original: `longest_losing_streak`, the statistic most
underestimated at long prices, and `median_max_drawdown` alongside the mean
(drawdown is skewed, so the mean alone misleads).

### Phase 3 — IPC layer
- [ ] Tauri commands wrapping every core entry point
- [ ] `tauri-specta` generating `src/lib/bindings.ts`
- [ ] Long-running sims as async commands with progress events (not frozen UI)
- [ ] Error type: `thiserror` in core → serializable enum at the boundary, no stringly-typed errors

### Phase 4 — Frontend shell + vertical slice
- [ ] Port the design system: CSS variables from `globals.css` (`--bg-primary` #0a0a0f,
      `--accent-green` #2ed573, `--accent-cyan` #00d4aa, …), JetBrains Mono / IBM Plex Sans,
      14px base — **plus a light mode**, which the web app lacks
- [ ] Port shared UI: `InputCard`, `FormRow`, `FormGroup`, `OutputSection`, `ResultRow`,
      `ResultLarge`, `EmptyState`, `InfoSection` → Svelte 5
- [ ] Calculator registry + routing
- [ ] **Four calculators end-to-end**, chosen to exercise every IPC shape:
      - Odds Converter — trivial sync, scalar in/out
      - Devig Calculator — array in, multi-method array out
      - Risk of Ruin — async Monte Carlo, progress events, survival chart
      - Odds-Range Variance — new module, heaviest chart surface
- [ ] **Gate: review the architecture here before mass-porting the other 17**

### Phase 5 — Odds-range / variance module (new)
The original idea. All simulation in Rust.
- [ ] Breakeven ladder — breakeven % vs price, with required win rate at a chosen edge
- [ ] CLV translator — cents of CLV vs **probability points** of CLV across the price range
      (-110→-130 is 20¢ but +4.1 pts; +400→+350 is 50¢ but only +2.2 pts)
- [ ] Bet-mix builder — blended breakeven, per-bet SD, variance contribution by price bucket
- [ ] Season simulator — Monte Carlo equity-curve fan at constant edge; P(losing season),
      median/p95 max drawdown, longest losing streak
- [ ] Charts in D3, data computed Rust-side

### Phase 6 — SQLite bet log
- [ ] rusqlite (bundled, WAL), migrations, schema versioning
- [ ] Log bets: date, market, price taken, closing price, stake, result
- [ ] Auto-compute CLV (in probability points) and realized ROI
- [ ] Feed the real book into the Phase 5 variance model — actual mix, not hypothetical

### Phase 7 — Probability visualizer
- [ ] Port `~/Code/probabilites-main/betting-probability-viz.html`: vig removal, margin/total
      scoring models, spread↔probability curve, EV-by-true-probability
- [ ] Math to Rust, D3 for render

### Phase 8 — Mass port
- [ ] Remaining 17 calculators against the settled Phase 4 architecture
- [ ] Drop the free/premium split — desktop app, everything unlocked
- [ ] Blog/MDX content: decide keep (bundled reference) or drop

---

## Open questions
- App name / bundle identifier / icon — `bettor-desktop` / `com.bettorcalculator.desktop` are placeholders
- Keep the 22 MDX blog posts as in-app reference docs, or leave them on the web?
- Does the web app stay live alongside this, or is it eventually replaced?

## Review
*(filled in as phases complete)*
