.DS_Store
node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Cargo workspace target dir — lives at the repo root, not under src-tauri/
/target

__pycache__/
*.py[cod]

/data/*
!/data/.gitkeep

# Local copies of exports — the shipped seed lives under seed/
/transactions*.csv
site/node_modules/
site/package-lock.json
site/smoke.mjs

# NOTE: golden-vector fixtures under crates/bettor-core/tests/fixtures/ are
# deliberately NOT ignored. They are the reference contract captured from the
# TypeScript implementation, and regenerating them on the fly would let a
# regression rewrite its own expected output.
